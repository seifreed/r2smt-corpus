# loop-memory assembly

The executable is built from `source/main.c` by the r2SMT benchmark gate.
This fixture intentionally exercises a bounded loop, a volatile memory write,
and a loop-carried data dependency.
